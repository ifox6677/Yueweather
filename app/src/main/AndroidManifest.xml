<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- For requesting background location access on Android 10 (API level 29+)-->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".WeatherApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:fontFamily = "sans-serif-light"
        android:theme="@style/AppTheme">

        <meta-data android:name="android.webkit.WebView.MetricsOptOut"
            android:value="true" />

        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!--WeatherApplication implements Configuration.Provider; the default
            AndroidXStartup initializer is disabled so on-demand initialization
            is the only path.-->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity android:name="org.zhangjq0908.weather.activities.RainViewerActivity"
            android:parentActivityName="org.zhangjq0908.weather.activities.ForecastCityActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="@string/RainViewer"/>

        <activity
            android:name="org.zhangjq0908.weather.activities.SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="org.zhangjq0908.weather.activities.ManageLocationsActivity"
            android:launchMode="singleTop"
            android:label="@string/activity_manage"
            android:theme="@style/AppTheme.NoActionBar"
            android:parentActivityName="org.zhangjq0908.weather.activities.ForecastCityActivity" />
        <activity
            android:name="org.zhangjq0908.weather.activities.ForecastCityActivity"
            android:launchMode="singleTop"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="org.zhangjq0908.weather.activities.SettingsActivity"
            android:label="@string/activity_settings_title"
            android:parentActivityName="org.zhangjq0908.weather.activities.ForecastCityActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="org.zhangjq0908.weather.activities.ForecastCityActivity" />

        </activity>

        <activity
            android:name="org.zhangjq0908.weather.activities.AboutActivity"
            android:label="@string/activity_about"
            android:theme="@style/AppTheme.NoActionBar" />


        <activity
            android:name="org.zhangjq0908.weather.firststart.TutorialActivity"
            android:theme="@style/AppTheme.NoActionBar" />

        <receiver android:name="org.zhangjq0908.weather.widget.WeatherWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_widget_info" />
        </receiver>

        <receiver android:name="org.zhangjq0908.weather.widget.WeatherDigitalClockWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_digital_clock_widget_info" />
        </receiver>

        <receiver android:name="org.zhangjq0908.weather.widget.WeatherWidget5day"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_widget_5day_info" />
        </receiver>

        <receiver android:name="org.zhangjq0908.weather.widget.WeatherWidgetAllInOne"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/weather_widget_all_in_one_info" />
        </receiver>

        <receiver android:name="org.zhangjq0908.weather.widget.RadarWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/radar_widget_info" />
        </receiver>

    </application>

</manifest>